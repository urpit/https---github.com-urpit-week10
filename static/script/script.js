console.log("node js function");
var os = require("os");

console.log(__dirname);
console.log(os.platform);
//if we want to run express application we use following command
//folder which has "package.json"
//npm install: used to intall a package from npm repository so that you cause it...
//npm prune: will look through your package.json file and remove any npm modules that are installed but not required for your project..
//npm list: shows a list of all packages installed for use by this application...
